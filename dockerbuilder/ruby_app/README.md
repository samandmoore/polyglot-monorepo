# Buildit for building a ruby app

this directory provides a set of files for properly building a ruby app
for deployment as a container.

it will:

* run `bundle exec rake`
* build a container for the ruby app
